%div{:class => "sidebar"}
  %h4 Feeds
  %div{:class => 'row', :id => 'z-feed-list'}
  - require 'uri'
  - locals[:feeds].each do |feed|
    - feed_title = feed.title || URI.parse(feed.feed_url).host
    %div{:class => "span8"}
      %span{:class => "label notice"}= "#{feed.feed_entries.size} "
      %strong
      %a{:data => {:feed_id => feed.key }}
        %b= "- #{feed_title}"
